<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="HardwareMonitorWinUI3.Views.MainWindow"
        x:Name="RootWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="using:HardwareMonitorWinUI3.UI"
        Title="Hardware Monitor WinUI 3">

    <Grid>
        <!-- Ressources locales pour les convertisseurs -->
        <Grid.Resources>
            <ui:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <ui:BoolToExpandIconConverter x:Key="BoolToExpandIconConverter"/>
        </Grid.Resources>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Zone réservée pour la barre de titre étendue -->
        <Grid Grid.Row="0" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            
            <!-- Titre de l'application -->
            <TextBlock Grid.Column="1"
                       Text="Hardware Monitor WinUI 3" 
                       FontSize="12" 
                       FontWeight="SemiBold"
                       Margin="16,0,0,0"
                       VerticalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
        </Grid>

        <!-- Header avec titre et contrôles -->
        <Border Grid.Row="1" 
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderThickness="0,0,0,1"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
            <Grid Margin="16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Titre avec icône -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <FontIcon x:Name="HardwareIcon" FontSize="20" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" Margin="0,0,8,0"/>
                    <TextBlock Text="Hardware Monitor" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>

                <!-- Sélecteur de Backdrop -->
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center" Margin="0,0,16,0">
                    <ComboBox x:Name="BackdropSelector"
                              SelectionChanged="BackdropSelector_SelectionChanged"
                              MinWidth="120"
                              VerticalAlignment="Center"
                              CornerRadius="8">
                    </ComboBox>
                </StackPanel>

                <!-- Boutons de vitesse avec indication visuelle simple + bouton paramètres -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Button x:Name="UltraButton" 
                            Click="SpeedButton_Click"
                            Content="ULTRA" Style="{StaticResource SpeedButtonActiveStyle}">
                    </Button>

                    <Button x:Name="RapideButton" 
                            Click="SpeedButton_Click"
                            Content="RAPIDE" Style="{StaticResource SpeedButtonInactiveStyle}">
                    </Button>

                    <Button x:Name="NormalButton" 
                            Click="SpeedButton_Click"
                            Content="NORMAL" Style="{StaticResource SpeedButtonInactiveStyle}">
                    </Button>
                    
                    <!-- Séparateur visuel -->
                    <Border x:Name="SpeedSeparator" Width="1" Height="24" Background="{ThemeResource DividerStrokeColorDefaultBrush}" 
                            VerticalAlignment="Center" Margin="4,0"/>
                    
                    <!-- Bouton RESET pour les min/max -->
                    <Button x:Name="ResetButton" 
                            Click="ResetButton_Click"
                            Content="RESET" Style="{StaticResource SpeedButtonStyle}">
                    </Button>
                    
                    <!-- Bouton de diagnostic hardware -->
                    <Button x:Name="HardwareDiagButton" 
                            Click="HardwareDiagButton_Click"
                            Content="DIAG" Style="{StaticResource SpeedButtonStyle}">
                    </Button>

                    <!-- Bouton paramètres déplacé ici, style icône seule -->
                    <Button x:Name="SettingsButton"
                            Style="{StaticResource SpeedButtonStyle}"
                            VerticalAlignment="Center"
                            MinWidth="32"
                            Padding="0"
                            Margin="0">
                        <FontIcon Glyph="&#xE713;" FontSize="16" FontFamily="Segoe MDL2 Assets" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Button.Flyout>
                            <Flyout Placement="BottomEdgeAlignedLeft">
                                <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                        BorderThickness="1"
                                        CornerRadius="8"
                                        Padding="8"
                                        MinWidth="180">
                                    <StackPanel Spacing="16">
                                        <!-- Section Affichage Hardware -->
                                        <StackPanel Spacing="8">
                                            <TextBlock Text="Affichage Hardware" FontSize="12" FontWeight="SemiBold"
                                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                            <StackPanel Spacing="4">
                                                <ToggleSwitch Header="CPU" IsOn="{x:Bind ViewModel.ShowCPU, Mode=TwoWay}"/>
                                                <ToggleSwitch Header="GPU" IsOn="{x:Bind ViewModel.ShowGPU, Mode=TwoWay}"/>
                                                <ToggleSwitch Header="Carte mère" IsOn="{x:Bind ViewModel.ShowMotherboard, Mode=TwoWay}"/>
                                                <ToggleSwitch Header="Stockage" IsOn="{x:Bind ViewModel.ShowStorage, Mode=TwoWay}"/>
                                                <ToggleSwitch Header="Mémoire" IsOn="{x:Bind ViewModel.ShowMemory, Mode=TwoWay}"/>
                                                <ToggleSwitch Header="Réseau" IsOn="{x:Bind ViewModel.ShowNetwork, Mode=TwoWay}"/>
                                                <ToggleSwitch Header="Contrôleurs" IsOn="{x:Bind ViewModel.ShowController, Mode=TwoWay}"/>
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>
                                </Border>
                            </Flyout>
                        </Button.Flyout>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Contenu principal avec binding vers le ViewModel -->
        <ScrollViewer Grid.Row="2"
                      ZoomMode="Disabled"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Padding="24,16,24,24">

            <StackPanel Spacing="16">
                <!-- Carte de statut principal avec binding -->
                <Border Style="{StaticResource StatusCardStyle}">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="8">
                            <TextBlock Text="État du système" FontSize="18" FontWeight="SemiBold"/>
                            <TextBlock Text="{x:Bind ViewModel.SystemStatusText, Mode=OneWay}"
                                      FontSize="14" 
                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <StackPanel Orientation="Horizontal" Spacing="12">
                                <TextBlock Text="{x:Bind ViewModel.UpsIndicator, Mode=OneWay}"
                                          FontSize="12" 
                                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                          FontWeight="SemiBold"/>
                                <TextBlock Text="{x:Bind ViewModel.BackdropIndicator, Mode=OneWay}"
                                          FontSize="12" 
                                          Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                          FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>

                        <ProgressRing Grid.Column="1" 
                                     IsActive="{x:Bind ViewModel.IsUIBusy, Mode=OneWay}"
                                     Width="32" 
                                     Height="32"
                                     VerticalAlignment="Center"/>
                    </Grid>
                </Border>

                <!-- Liste des composants hardware -->
                <ListView x:Name="HardwareList"
                         ItemsSource="{x:Bind ViewModel.HardwareNodes, Mode=OneWay}"
                         ScrollViewer.VerticalScrollBarVisibility="Auto"
                         ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                         ScrollViewer.IsScrollInertiaEnabled="False"
                         SelectionMode="None"
                         IsItemClickEnabled="False">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="0"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Style="{StaticResource HardwareCardStyle}">

                                <StackPanel Spacing="12">
                                    <!-- Header du composant avec bouton collapse/expand -->
                                    <Button Background="Transparent" 
                                           BorderThickness="0"
                                           Padding="0"
                                           HorizontalAlignment="Stretch"
                                           HorizontalContentAlignment="Stretch"
                                           Click="ToggleExpand_Click"
                                           Tag="{Binding}">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="Auto"/>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="Auto"/>
                                            </Grid.ColumnDefinitions>

                                            <!-- Icône expand/collapse -->
                                            <FontIcon Grid.Column="0" 
                                                      x:Name="ExpandIcon"
                                                      FontFamily="Segoe MDL2 Assets"
                                                      Glyph="{Binding IsExpanded, Converter={StaticResource BoolToExpandIconConverter}}" 
                                                      FontSize="14" 
                                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                      Margin="0,0,8,0"
                                                      VerticalAlignment="Center"/>

                                            <!-- Icône du composant -->
                                            <FontIcon Grid.Column="1" 
                                                     Glyph="&#xE950;" 
                                                     FontSize="16" 
                                                     Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                                     Margin="0,0,12,0"
                                                     VerticalAlignment="Center"/>

                                            <!-- Nom du composant -->
                                            <TextBlock Grid.Column="2" 
                                                      Text="{Binding Name}" 
                                                      FontSize="16" 
                                                      FontWeight="SemiBold"
                                                      VerticalAlignment="Center"/>

                                            <!-- Badge nombre de capteurs -->
                                            <TextBlock Grid.Column="3"
                                                       Text="{Binding Sensors.Count}"
                                                       FontSize="12"
                                                       FontWeight="SemiBold"
                                                       Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                                       VerticalAlignment="Center"
                                                       Margin="8,0,0,0"/>
                                        </Grid>
                                    </Button>

                                    <!-- Groupes de capteurs organisés -->
                                    <ListView ItemsSource="{Binding SensorGroups}"
                                              Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVisibilityConverter}}"
                                              SelectionMode="None"
                                              IsItemClickEnabled="False">
                                        <ListView.ItemTemplate>
                                            <DataTemplate>
                                                <Border Style="{StaticResource SensorGroupStyle}">
                                                    <StackPanel>
                                                        <!-- En-tête du groupe -->
                                                        <Button Background="Transparent" 
                                                               BorderThickness="0"
                                                               Padding="0"
                                                               HorizontalAlignment="Stretch"
                                                               HorizontalContentAlignment="Stretch"
                                                               Click="ToggleGroup_Click"
                                               Tag="{Binding}">
                                                            <Grid>
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>

                                                                <!-- Icône expand/collapse -->
                                                                <FontIcon Grid.Column="0" 
                                                                         x:Name="GroupExpandIcon"
                                                                         FontFamily="Segoe MDL2 Assets"
                                                                         Glyph="{Binding IsExpanded, Converter={StaticResource BoolToExpandIconConverter}}" 
                                                                         FontSize="14" 
                                                                         Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                         Margin="0,0,8,0"
                                                                         VerticalAlignment="Center"/>

                                                                <!-- Icône de la catégorie -->
                                                                <TextBlock Grid.Column="1" 
                                                                          Text="{Binding CategoryIcon}" 
                                                                          FontSize="16" 
                                                                          Margin="0,0,12,0"
                                                                          VerticalAlignment="Center"/>

                                                                <!-- Nom de la catégorie -->
                                                                <TextBlock Grid.Column="2" 
                                                                          Text="{Binding CategoryName}" 
                                                                          FontSize="14" 
                                                                          FontWeight="SemiBold"
                                                                          VerticalAlignment="Center"/>

                                                                <!-- Nombre de capteurs -->
                                                                <TextBlock Grid.Column="3"
                                                                           Text="{Binding Sensors.Count}"
                                                                           FontSize="12"
                                                                           FontWeight="SemiBold"
                                                                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                                                           VerticalAlignment="Center"
                                                                           Margin="8,0,0,0"/>
                                                            </Grid>
                                                        </Button>

                                                        <!-- Capteurs du groupe -->
                                                        <ItemsControl ItemsSource="{Binding Sensors}"
                                                                     Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVisibilityConverter}}">
                                                            <ItemsControl.ItemsPanel>
                                                                <ItemsPanelTemplate>
                                                                    <ItemsWrapGrid Orientation="Horizontal"/>
                                                                </ItemsPanelTemplate>
                                                            </ItemsControl.ItemsPanel>
                                                            <ItemsControl.ItemTemplate>
                                                                <DataTemplate>
                                                                    <Border Style="{StaticResource SensorCardStyle}">
                                                                        <Grid>
                                                                            <Grid.RowDefinitions>
                                                                                <RowDefinition Height="Auto"/>
                                                                                <RowDefinition Height="Auto"/>
                                                                                <RowDefinition Height="Auto"/>
                                                                            </Grid.RowDefinitions>

                                                                            <!-- Nom du capteur avec icône -->
                                                                            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="6" Margin="0,0,0,2">
                                                                                <TextBlock Text="{Binding Icon}" 
                                                                                          FontSize="12" 
                                                                                          VerticalAlignment="Center"/>
                                                                                <TextBlock Text="{Binding Name}" 
                                                                                          FontSize="13" 
                                                                                          FontWeight="SemiBold"
                                                                                          Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                                                                          TextTrimming="CharacterEllipsis"
                                                                                          MaxLines="1"
                                                                                          VerticalAlignment="Center"/>
                                                                            </StackPanel>

                                                                            <!-- Valeur actuelle -->
                                                                            <TextBlock Grid.Row="1" 
                                                                                      Text="{Binding Value}" 
                                                                                      FontSize="18" 
                                                                                      FontWeight="Bold"
                                                                                      Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                                                                      VerticalAlignment="Center"
                                                                                       Margin="0,2,0,2"/>

                                                                            <!-- Min/Max -->
                                                                            <StackPanel Grid.Row="2" 
                                                                                       Orientation="Horizontal" 
                                                                                       Spacing="12"
                                                                                       Margin="0,2,0,0">
                                                                                <TextBlock Text="{Binding MinValue}" 
                                                                                          FontSize="11" 
                                                                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                                          FontWeight="Medium"/>
                                                                                <TextBlock Text="{Binding MaxValue}" 
                                                                                          FontSize="11" 
                                                                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                                          FontWeight="Medium"/>
                                                                            </StackPanel>
                                                                        </Grid>
                                                                    </Border>
                                                                </DataTemplate>
                                                            </ItemsControl.ItemTemplate>
                                                        </ItemsControl>
                                                    </StackPanel>
                                                </Border>
                                            </DataTemplate>
                                        </ListView.ItemTemplate>
                                    </ListView>
                                </StackPanel>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window> 