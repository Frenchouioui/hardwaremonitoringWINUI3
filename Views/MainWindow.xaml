<?xml version="1.0" encoding="utf-8"?>
<Window x:Class="HardwareMonitorWinUI3.Views.MainWindow"
        x:Name="RootWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="using:HardwareMonitorWinUI3.UI"
        xmlns:models="using:HardwareMonitorWinUI3.Models"
        Title="Hardware Monitor WinUI 3">

    <Grid>
        <Grid.Resources>
            <ui:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter"/>
            <ui:BoolToExpandIconConverter x:Key="BoolToExpandIconConverter"/>
            <ui:BoolToAccentBrushConverter x:Key="BoolToAccentBrushConverter"/>
            <ui:ViewModeToVisibilityConverter x:Key="ViewModeToVisibilityConverter"/>
            <ui:TemperatureUnitToBoolConverter x:Key="TemperatureUnitToBoolConverter"/>
            
            <DataTemplate x:Key="TreeHardwareNodeTemplate" x:DataType="models:HardwareNode">
                <Expander IsExpanded="{Binding IsExpanded, Mode=TwoWay}" 
                          HorizontalAlignment="Stretch"
                          HorizontalContentAlignment="Stretch"
                          CornerRadius="8"
                          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                          BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                          BorderThickness="1"
                          Margin="0,0,0,8">
                    <Expander.Header>
                        <Grid Margin="0,4">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <FontIcon Grid.Column="0" Glyph="{Binding CategoryIcon}" FontSize="16" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" Margin="0,0,12,0" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="1" Text="{Binding Name}" FontWeight="SemiBold" FontSize="14" VerticalAlignment="Center"/>
                            <TextBlock Grid.Column="2" Text="{Binding SensorCount}" FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="12,0,0,0" VerticalAlignment="Center"/>
                        </Grid>
                    </Expander.Header>
                    <Expander.Content>
                        <ItemsControl ItemsSource="{Binding SensorGroups}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate x:DataType="models:SensorGroup">
                                    <Expander IsExpanded="{Binding IsExpanded, Mode=TwoWay}"
                                              HorizontalAlignment="Stretch"
                                              HorizontalContentAlignment="Stretch"
                                              CornerRadius="6"
                                              Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                                              Margin="0,0,0,6">
                                        <Expander.Header>
                                            <Grid Margin="0,2">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="*"/>
                                                </Grid.ColumnDefinitions>
                                                <FontIcon Grid.Column="0" Glyph="{Binding CategoryIcon}" FontSize="14" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                                                <TextBlock Grid.Column="1" Text="{Binding CategoryName}" FontWeight="Medium" FontSize="13" VerticalAlignment="Center"/>
                                            </Grid>
                                        </Expander.Header>
                                        <Expander.Content>
                                            <Grid Margin="8,4,8,8">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                    <ColumnDefinition Width="Auto"/>
                                                </Grid.ColumnDefinitions>
                                                <Grid.RowDefinitions>
                                                    <RowDefinition Height="Auto"/>
                                                    <RowDefinition Height="Auto"/>
                                                </Grid.RowDefinitions>
                                                <TextBlock Grid.Column="0" Text="Name" FontSize="11" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorTertiaryBrush}" Margin="0,0,0,8"/>
                                                <TextBlock Grid.Column="1" Text="Value" FontSize="11" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorTertiaryBrush}" MinWidth="100" TextAlignment="Right" Margin="0,0,0,8"/>
                                                <TextBlock Grid.Column="2" Text="Min" FontSize="11" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorTertiaryBrush}" MinWidth="120" TextAlignment="Right" Margin="0,0,0,8"/>
                                                <TextBlock Grid.Column="3" Text="Max" FontSize="11" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorTertiaryBrush}" MinWidth="120" TextAlignment="Right" Margin="0,0,0,8"/>
                                                <ItemsControl Grid.Row="1" Grid.ColumnSpan="4" ItemsSource="{Binding Sensors}">
                                                    <ItemsControl.ItemTemplate>
                                                        <DataTemplate x:DataType="models:SensorData">
                                                            <Grid Margin="0,2,0,0">
                                                                <Grid.ColumnDefinitions>
                                                                    <ColumnDefinition Width="*"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                    <ColumnDefinition Width="Auto"/>
                                                                </Grid.ColumnDefinitions>
                                                                <TextBlock Grid.Column="0" Text="{Binding Name}" FontSize="12" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding Name}" VerticalAlignment="Center"/>
                                                                <TextBlock Grid.Column="1" Text="{Binding Value}" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" MinWidth="100" TextAlignment="Right" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding Value}" VerticalAlignment="Center"/>
                                                                <TextBlock Grid.Column="2" Text="{Binding MinValue}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" MinWidth="120" TextAlignment="Right" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding MinValue}" VerticalAlignment="Center"/>
                                                                <TextBlock Grid.Column="3" Text="{Binding MaxValue}" FontSize="11" Foreground="{ThemeResource TextFillColorSecondaryBrush}" MinWidth="120" TextAlignment="Right" TextTrimming="CharacterEllipsis" ToolTipService.ToolTip="{Binding MaxValue}" VerticalAlignment="Center"/>
                                                            </Grid>
                                                        </DataTemplate>
                                                    </ItemsControl.ItemTemplate>
                                                </ItemsControl>
                                            </Grid>
                                        </Expander.Content>
                                    </Expander>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </Expander.Content>
                </Expander>
            </DataTemplate>

            <DataTemplate x:Key="SensorDataTemplate" x:DataType="models:SensorData">
                <Border Style="{StaticResource SensorCardStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="6" Margin="0,0,0,2">
                            <FontIcon Glyph="{Binding Icon}"
                                      FontFamily="Segoe MDL2 Assets"
                                      FontSize="12"
                                      VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding Name}"
                                      FontSize="13"
                                      FontWeight="SemiBold"
                                      Foreground="{ThemeResource TextFillColorPrimaryBrush}"
                                      TextTrimming="CharacterEllipsis"
                                      MaxLines="1"
                                      VerticalAlignment="Center"/>
                        </StackPanel>

                        <TextBlock Grid.Row="1"
                                   Text="{Binding Value}"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                   Margin="0,2,0,2"/>

                        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="12" Margin="0,2,0,0">
                            <TextBlock Text="{Binding MinValue}" FontSize="11"
                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                      FontWeight="Medium"/>
                            <TextBlock Text="{Binding MaxValue}" FontSize="11"
                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                      FontWeight="Medium"/>
                        </StackPanel>
                    </Grid>
                </Border>
            </DataTemplate>

            <DataTemplate x:Key="SensorGroupTemplate" x:DataType="models:SensorGroup">
                <Border Style="{StaticResource SensorGroupStyle}">
                    <StackPanel>
                        <Button Background="Transparent"
                               BorderThickness="0"
                               Padding="0"
                               HorizontalAlignment="Stretch"
                               HorizontalContentAlignment="Stretch"
                               Click="ToggleGroup_Click"
                               Tag="{Binding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <FontIcon Grid.Column="0"
                                         FontFamily="Segoe MDL2 Assets"
                                         Glyph="{Binding IsExpanded, Converter={StaticResource BoolToExpandIconConverter}}"
                                         FontSize="14"
                                         Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                         Margin="0,0,8,0"
                                         VerticalAlignment="Center"/>

                                <FontIcon Grid.Column="1"
                                          Glyph="{Binding CategoryIcon}"
                                          FontFamily="Segoe MDL2 Assets"
                                          FontSize="16"
                                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                          Margin="0,0,12,0"
                                          VerticalAlignment="Center"/>

                                <TextBlock Grid.Column="2"
                                          Text="{Binding CategoryName}"
                                          FontSize="14"
                                          FontWeight="SemiBold"
                                          VerticalAlignment="Center"/>

                                <TextBlock Grid.Column="3"
                                           Text="{Binding SensorCount}"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                           VerticalAlignment="Center"
                                           Margin="8,0,0,0"/>
                            </Grid>
                        </Button>

                        <ItemsControl ItemsSource="{Binding Sensors}"
                                     Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVisibilityConverter}}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsWrapGrid Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <StaticResource ResourceKey="SensorDataTemplate"/>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </DataTemplate>

            <DataTemplate x:Key="SubSensorGroupTemplate" x:DataType="models:SensorGroup">
                <Border Style="{StaticResource SensorGroupStyle}">
                    <StackPanel>
                        <Button Background="Transparent"
                                BorderThickness="0"
                                Padding="0"
                                HorizontalAlignment="Stretch"
                                HorizontalContentAlignment="Stretch"
                                Click="ToggleGroup_Click"
                                Tag="{Binding}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <FontIcon Grid.Column="0"
                                          FontFamily="Segoe MDL2 Assets"
                                          Glyph="{Binding IsExpanded, Converter={StaticResource BoolToExpandIconConverter}}"
                                          FontSize="12"
                                          Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                          Margin="0,0,8,0"
                                          VerticalAlignment="Center"/>

                                <FontIcon Grid.Column="1"
                                          Glyph="{Binding CategoryIcon}"
                                          FontFamily="Segoe MDL2 Assets"
                                          FontSize="14"
                                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                          Margin="0,0,12,0"
                                          VerticalAlignment="Center"/>

                                <TextBlock Grid.Column="2"
                                          Text="{Binding CategoryName}"
                                          FontSize="13"
                                          FontWeight="SemiBold"
                                          VerticalAlignment="Center"/>

                                <TextBlock Grid.Column="3"
                                           Text="{Binding SensorCount}"
                                           FontSize="11"
                                           FontWeight="SemiBold"
                                           Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                           VerticalAlignment="Center"
                                           Margin="8,0,0,0"/>
                            </Grid>
                        </Button>

                        <ItemsControl ItemsSource="{Binding Sensors}"
                                     Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVisibilityConverter}}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <ItemsWrapGrid Orientation="Horizontal"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <StaticResource ResourceKey="SensorDataTemplate"/>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>
                </Border>
            </DataTemplate>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="32"/>
            <RowDefinition Height="48"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Title bar area -->
        <Grid Grid.Row="0" Background="Transparent">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="1"
                       Text="{x:Bind Title, Mode=OneWay}"
                       FontSize="12"
                       FontWeight="SemiBold"
                       Margin="16,0,0,0"
                       VerticalAlignment="Center"
                       Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
        </Grid>

        <!-- Header -->
        <Border Grid.Row="1"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                BorderThickness="0,0,0,1"
                BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}">
            <Grid Margin="16,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                    <FontIcon Glyph="&#xE950;" FontSize="20" Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}" Margin="0,0,8,0"/>
                    <TextBlock Text="Hardware Monitor" FontSize="16" FontWeight="SemiBold" VerticalAlignment="Center"/>
                </StackPanel>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="12" VerticalAlignment="Center" Margin="0,0,16,0">
                    <ComboBox x:Name="BackdropSelector"
                              SelectionChanged="BackdropSelector_SelectionChanged"
                              MinWidth="120"
                              VerticalAlignment="Center"
                              CornerRadius="8">
                        <ComboBoxItem>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE91C;" FontSize="12"/>
                                <TextBlock Text="Acrylic" FontSize="12"/>
                            </StackPanel>
                        </ComboBoxItem>
                        <ComboBoxItem>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE7E8;" FontSize="12"/>
                                <TextBlock Text="Mica" FontSize="12"/>
                            </StackPanel>
                        </ComboBoxItem>
                        <ComboBoxItem>
                            <StackPanel Orientation="Horizontal" Spacing="8">
                                <FontIcon Glyph="&#xE7E8;" FontSize="12"/>
                                <TextBlock Text="Mica Alt" FontSize="12"/>
                            </StackPanel>
                        </ComboBoxItem>
                    </ComboBox>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                    <Button Command="{Binding ViewModel.ChangeSpeedCommand, ElementName=RootWindow}"
                            CommandParameter="250"
                            BorderBrush="{Binding ViewModel.IsUltraActive, ElementName=RootWindow, Converter={StaticResource BoolToAccentBrushConverter}}"
                            Style="{StaticResource SpeedButtonStyle}">
                        <TextBlock Text="ULTRA" FontSize="11" FontWeight="SemiBold"/>
                    </Button>

                    <Button Command="{Binding ViewModel.ChangeSpeedCommand, ElementName=RootWindow}"
                            CommandParameter="500"
                            BorderBrush="{Binding ViewModel.IsFastActive, ElementName=RootWindow, Converter={StaticResource BoolToAccentBrushConverter}}"
                            Style="{StaticResource SpeedButtonStyle}">
                        <TextBlock Text="FAST" FontSize="11" FontWeight="SemiBold"/>
                    </Button>

                    <Button Command="{Binding ViewModel.ChangeSpeedCommand, ElementName=RootWindow}"
                            CommandParameter="1000"
                            BorderBrush="{Binding ViewModel.IsNormalActive, ElementName=RootWindow, Converter={StaticResource BoolToAccentBrushConverter}}"
                            Style="{StaticResource SpeedButtonStyle}">
                        <TextBlock Text="NORMAL" FontSize="11" FontWeight="SemiBold"/>
                    </Button>

                    <Border Width="1" Height="24" Background="{ThemeResource DividerStrokeColorDefaultBrush}"
                            VerticalAlignment="Center" Margin="4,0"/>

                    <Button Command="{Binding ViewModel.ResetMinMaxCommand, ElementName=RootWindow}"
                            Style="{StaticResource SpeedButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <FontIcon Glyph="&#xE845;" FontSize="12" Margin="0,0,4,0"/>
                            <TextBlock Text="RESET" FontSize="11" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>

                    <Button Command="{Binding ViewModel.RunDiagnosticCommand, ElementName=RootWindow}"
                             Style="{StaticResource SpeedButtonStyle}">
                        <StackPanel Orientation="Horizontal">
                            <FontIcon Glyph="&#xE9D9;" FontSize="12" Margin="0,0,4,0"/>
                            <TextBlock Text="DIAG" FontSize="11" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>

                    <Button x:Name="ViewModeToggle"
                            Click="ViewModeToggle_Click"
                            Style="{StaticResource SpeedButtonStyle}"
                            ToolTipService.ToolTip="Toggle Cards/Tree View">
                        <FontIcon Glyph="&#xE8FD;" FontSize="14" FontFamily="Segoe MDL2 Assets"/>
                    </Button>

                    <Button x:Name="SettingsButton"
                            Style="{StaticResource SpeedButtonStyle}"
                            VerticalAlignment="Center"
                            MinWidth="32"
                            Padding="0"
                            Margin="0">
                        <FontIcon Glyph="&#xE713;" FontSize="16" FontFamily="Segoe MDL2 Assets" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        <Button.Flyout>
                             <Flyout Placement="BottomEdgeAlignedLeft">
                                <StackPanel Spacing="12" MinWidth="220">
                                    <TextBlock Text="Hardware Display" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                                    <ToggleSwitch Header="Motherboard" IsOn="{x:Bind ViewModel.ShowMotherboard, Mode=TwoWay}"/>
                                    <ToggleSwitch Header="CPU" IsOn="{x:Bind ViewModel.ShowCPU, Mode=TwoWay}"/>
                                    <ToggleSwitch Header="Memory" IsOn="{x:Bind ViewModel.ShowMemory, Mode=TwoWay}"/>
                                    <ToggleSwitch Header="GPU" IsOn="{x:Bind ViewModel.ShowGPU, Mode=TwoWay}"/>
                                    <ToggleSwitch Header="Storage" IsOn="{x:Bind ViewModel.ShowStorage, Mode=TwoWay}"/>
                                    <ToggleSwitch Header="Network" IsOn="{x:Bind ViewModel.ShowNetwork, Mode=TwoWay}"/>
                                    <ToggleSwitch Header="Controller" IsOn="{x:Bind ViewModel.ShowController, Mode=TwoWay}"/>
                                    <ToggleSwitch Header="Battery" IsOn="{x:Bind ViewModel.ShowBattery, Mode=TwoWay}"/>
                                    <ToggleSwitch Header="Power Supply" IsOn="{x:Bind ViewModel.ShowPsu, Mode=TwoWay}"/>
                                    
                                    <Rectangle Height="1" Fill="{ThemeResource DividerStrokeColorDefaultBrush}" Margin="0,4"/>
                                    
                                    <TextBlock Text="Temperature Unit" FontSize="12" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorPrimaryBrush}"/>
                                    <StackPanel Orientation="Horizontal" Spacing="16">
                                        <RadioButton Content="Celsius" GroupName="TempUnit" IsChecked="{x:Bind ViewModel.IsCelsius, Mode=TwoWay}"/>
                                        <RadioButton Content="Fahrenheit" GroupName="TempUnit" IsChecked="{x:Bind ViewModel.IsFahrenheit, Mode=TwoWay}"/>
                                    </StackPanel>
                                </StackPanel>
                            </Flyout>
                        </Button.Flyout>
                    </Button>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Main content: Cards View or Tree View -->
        <Grid Grid.Row="2">
            <!-- Cards View -->
            <ListView x:Name="HardwareList"
                      ItemsSource="{Binding ViewModel.HardwareNodes, ElementName=RootWindow, Mode=OneWay}"
                      SelectionMode="None"
                      IsItemClickEnabled="False"
                      Padding="24,16,24,24"
                      Visibility="{Binding ViewModel.CurrentViewMode, ElementName=RootWindow, Converter={StaticResource ViewModeToVisibilityConverter}, ConverterParameter=Cards}">

            <!-- Status card as ListView header so it scrolls with content -->
            <ListView.Header>
                <Border Style="{StaticResource StatusCardStyle}" Margin="0,0,0,16">
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Column="0" Spacing="8">
                            <TextBlock Text="System Status" FontSize="18" FontWeight="SemiBold"/>
                            <TextBlock Text="{Binding ViewModel.SystemStatusText, ElementName=RootWindow, Mode=OneWay}"
                                      FontSize="14"
                                      Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            <StackPanel Orientation="Horizontal" Spacing="12">
                                <TextBlock Text="{Binding ViewModel.UpsIndicator, ElementName=RootWindow, Mode=OneWay}"
                                          FontSize="12"
                                          Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                          FontWeight="SemiBold"/>
                                <TextBlock Text="{Binding ViewModel.BackdropIndicator, ElementName=RootWindow, Mode=OneWay}"
                                          FontSize="12"
                                          Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                          FontWeight="SemiBold"/>
                            </StackPanel>
                        </StackPanel>

                        <ProgressRing Grid.Column="1"
                                     IsActive="{Binding ViewModel.IsUIBusy, ElementName=RootWindow, Mode=OneWay}"
                                     Width="32"
                                     Height="32"
                                     VerticalAlignment="Center"/>
                    </Grid>
                </Border>
            </ListView.Header>

            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem" BasedOn="{StaticResource DefaultListViewItemStyle}">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="0"/>
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:HardwareNode">
                    <Border Style="{StaticResource HardwareCardStyle}">
                        <StackPanel Spacing="12">
                            <Button Background="Transparent"
                                   BorderThickness="0"
                                   Padding="0"
                                   HorizontalAlignment="Stretch"
                                   HorizontalContentAlignment="Stretch"
                                   Click="ToggleExpand_Click"
                                   Tag="{Binding}">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>

                                    <FontIcon Grid.Column="0"
                                             Glyph="{Binding IsExpanded, Converter={StaticResource BoolToExpandIconConverter}}"
                                             FontSize="14"
                                             Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                             Margin="0,0,8,0"
                                             VerticalAlignment="Center"/>

                                    <FontIcon Grid.Column="1"
                                              Glyph="{x:Bind CategoryIcon, Mode=OneWay}"
                                              FontSize="16"
                                              Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                              Margin="0,0,12,0"
                                              VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="2"
                                              Text="{Binding Name}"
                                              FontSize="16"
                                              FontWeight="SemiBold"
                                              VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="3"
                                               Text="{Binding SensorCount}"
                                               FontSize="12"
                                               FontWeight="SemiBold"
                                               Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                               VerticalAlignment="Center"
                                               Margin="8,0,0,0"/>
                                </Grid>
                            </Button>

                            <ItemsControl ItemsSource="{Binding SensorGroups}"
                                         Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVisibilityConverter}}"
                                         ItemTemplate="{StaticResource SensorGroupTemplate}"/>

                            <ItemsControl ItemsSource="{Binding SubHardware}"
                                         Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVisibilityConverter}}"
                                         Margin="16,8,0,0">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate x:DataType="models:HardwareNode">
                                        <Border Style="{StaticResource HardwareCardStyle}">
                                            <StackPanel Spacing="12">
                                                <Button Background="Transparent"
                                                        BorderThickness="0"
                                                        Padding="0"
                                                        HorizontalAlignment="Stretch"
                                                        HorizontalContentAlignment="Stretch"
                                                        Click="ToggleExpand_Click"
                                                        Tag="{Binding}">
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>

                                                        <FontIcon Grid.Column="0"
                                                                  Glyph="{Binding IsExpanded, Converter={StaticResource BoolToExpandIconConverter}}"
                                                                  FontSize="12"
                                                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                                  Margin="0,0,8,0"
                                                                  VerticalAlignment="Center"/>

                                                        <FontIcon Grid.Column="1"
                                                                  Glyph="{x:Bind CategoryIcon, Mode=OneWay}"
                                                                  FontSize="14"
                                                                  Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                                                  Margin="0,0,12,0"
                                                                  VerticalAlignment="Center"/>

                                                        <TextBlock Grid.Column="2"
                                                                  Text="{Binding Name}"
                                                                  FontSize="14"
                                                                  FontWeight="SemiBold"
                                                                  VerticalAlignment="Center"/>

                                                        <TextBlock Grid.Column="3"
                                                                   Text="{Binding SensorCount}"
                                                                   FontSize="11"
                                                                   FontWeight="SemiBold"
                                                                   Foreground="{ThemeResource AccentTextFillColorPrimaryBrush}"
                                                                   VerticalAlignment="Center"
                                                                   Margin="8,0,0,0"/>
                                                    </Grid>
                                                </Button>

                                                <ItemsControl ItemsSource="{Binding SensorGroups}"
                                                             Visibility="{Binding IsExpanded, Converter={StaticResource BoolToVisibilityConverter}}"
                                                             ItemTemplate="{StaticResource SubSensorGroupTemplate}"/>
                                            </StackPanel>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </StackPanel>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
        <!-- Tree View -->
        <ScrollViewer Visibility="{Binding ViewModel.CurrentViewMode, ElementName=RootWindow, Converter={StaticResource ViewModeToVisibilityConverter}, ConverterParameter=Tree}"
                      Padding="24,16,24,24">
            <ItemsControl ItemsSource="{Binding ViewModel.HardwareNodes, ElementName=RootWindow, Mode=OneWay}"
                          ItemTemplate="{StaticResource TreeHardwareNodeTemplate}"/>
        </ScrollViewer>
        </Grid>
    </Grid>
</Window>
